---
description: Alias /memory — same as /bulma-memory
argument-hint: "[--project] [<note>]"
---

# /memory

Alias of **`/bulma-memory`**. Use arguments from the user request.

Follow **`../skills/bulma-memory/SKILL.md`** in full —
this alias defines no steps of its own.
