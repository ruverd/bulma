---
description: Alias /reviewer — same as /bulma-reviewer
argument-hint: "<PR url | owner/repo#N | branch>"
---

# /reviewer

Alias of **`/bulma-reviewer`**. Use arguments from the user request.

Follow **`../skills/bulma-reviewer/SKILL.md`** in full —
this alias defines no steps of its own.
