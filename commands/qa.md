---
description: Alias /qa — same as /ruver-qa (agent-browser or HTTP)
argument-hint: "<PR url or owner/repo#N>"
---

# /qa

Alias of **`/ruver-qa`**. Use arguments from the user request.

Follow **`../skills/ruver-qa/SKILL.md`** in full —
this alias defines no steps of its own.
