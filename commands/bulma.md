---
description: Route the request to the right ruver graph and gate its forks with Jev decisions
argument-hint: "<empty | ticket | PR url | text | power <level> | report | resume>"
---

# /bulma

Follow **`../skills/bulma/SKILL.md`** in full. Use the arguments from the
user request. This alias defines no steps of its own.

Needs `TYPESAFE_API_KEY`. Without it the skill prints the requirement and
stops; run `/developer`, `/qa`, `/reviewer`, `/lstm` or `/ruver-triage`
directly instead.
