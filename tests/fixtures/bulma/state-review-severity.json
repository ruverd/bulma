{"finding":"src/billing/invoice.ts:88 — the catch around createInvoice logs and returns null, so a failed charge reads as an empty invoice upstream.","diff_hunk":"@@ -80,9 +80,14 @@ export async function createInvoice(input: Input) {\n+  try {\n+    return await gateway.charge(input)\n+  } catch (err) {\n+    logger.warn(err)\n+    return null\n+  }","repo_rules":"CLAUDE.md: never swallow an error from the payment gateway; surface it to the caller.","neighbor_pattern":"Every other gateway call in src/billing rethrows a GatewayError and the route maps it to a 502.","pr":{"description":"Add invoice creation behind the billing flag.","acceptance_criteria":["a failed charge surfaces an error to the caller"]}}
